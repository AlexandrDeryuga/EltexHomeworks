Обмен работает, но
не получилось отловить событие закрытия клиентом своего сокета,
на сервере не срабатывает данное условие:
 if(events[i].events & EPOLLHUP) {
     printf("TCP/UDP сервер>Клиент с дескриптором %d разорвал соединение\n",fd);sleep(5);


