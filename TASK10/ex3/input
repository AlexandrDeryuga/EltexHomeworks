FILE *nf=NULL;
  
  int i =0,r=0;
  if (args[0] == NULL) { return 1; }  //ничего не ввели
  if (strcmp(args[0],"exit")==0) ExitFree(0); // покинем интерпетатор
  
                         
  //if(args[0][0]='>')
  //if(args[0][0]='<')
